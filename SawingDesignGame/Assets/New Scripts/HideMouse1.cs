using UnityEngine;

public class HideMouse1 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    private void Start()
    {
        Screen.SetResolution(1920, 1080, true);
    }
}
